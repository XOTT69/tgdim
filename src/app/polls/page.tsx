"use client";
import { useState } from "react";
import PageHeader from "@/components/PageHeader";
import { polls } from "@/lib/demo-data";
export default function PollsPage() { const [selected, setSelected] = useState<number | null>(null); const poll = polls[0]; const total = poll.votes.reduce((a,b) => a+b, 0) + (selected === null ? 0 : 1); return <><PageHeader title="Голосування" /><article className="card"><p className="text-xs font-medium text-blue-600">АКТИВНЕ · {poll.closing}</p><h2 className="mt-2 text-lg font-semibold">{poll.question}</h2><div className="mt-4 space-y-2">{poll.options.map((option, index) => <button key={option} onClick={() => setSelected(index)} className={`w-full rounded-xl border p-3 text-left text-sm ${selected === index ? "border-blue-500 bg-blue-50" : "border-slate-200"}`}><div className="flex justify-between"><span>{option}</span>{selected !== null && <b>{Math.round(((poll.votes[index] + (selected === index ? 1 : 0)) / total) * 100)}%</b>}</div>{selected !== null && <div className="mt-2 h-1.5 rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-500" style={{ width: `${((poll.votes[index] + (selected === index ? 1 : 0)) / total) * 100}%` }} /></div>}</button>)}</div><p className="mt-3 text-xs text-slate-400">{total} голосів · один голос від мешканця</p></article><p className="mt-4 text-center text-sm text-slate-400">Нові голосування з’являться тут.</p></>; }
