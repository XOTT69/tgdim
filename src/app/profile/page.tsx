"use client";
import { useTelegramWebApp } from "@/lib/use-telegram";
import PageHeader from "@/components/PageHeader";
export default function ProfilePage() { const { user, ready } = useTelegramWebApp(); if (!ready) return null; const name = user ? `${user.first_name}${user.username ? ` · @${user.username}` : ""}` : "Гість"; return <><PageHeader title="Профіль" /><div className="card flex items-center gap-3"><div className="flex h-14 w-14 items-center justify-center rounded-full bg-blue-100 text-2xl">👤</div><div><h2 className="font-semibold">{name}</h2><p className="text-sm text-slate-500">{user ? "Ваш Telegram-профіль" : "Відкрийте застосунок через Telegram"}</p></div></div><form className="card mt-4 space-y-4"><h2 className="font-semibold">Ваш будинок</h2><label className="block text-sm font-medium">Під’їзд<input className="field mt-1" placeholder="Наприклад, 2" inputMode="numeric" /></label><label className="block text-sm font-medium">Квартира<input className="field mt-1" placeholder="Наприклад, 48" inputMode="numeric" /></label><label className="flex items-center justify-between gap-4 text-sm font-medium">Сповіщення про важливе<input type="checkbox" defaultChecked className="h-5 w-5 accent-blue-600" /></label><button type="button" className="primary-button w-full">Зберегти зміни</button></form></>; }
